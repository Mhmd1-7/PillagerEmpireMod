plugins {
    id 'fabric-loom' version '1.6-SNAPSHOT' // Updated for 1.20.1 stability
}

// ... (keep other sections but update these key parts)

loom {
    mods {
        "pillagerempire" { // Must match fabric.mod.json
            sourceSet sourceSets.main
        }
    }
}

dependencies {
    // Essential 1.20.1 dependencies
    minecraft "com.mojang:minecraft:1.20.1"
    mappings "net.fabricmc:yarn:1.20.1+build.10:v2"
    modImplementation "net.fabricmc:fabric-loader:1.21.6"
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.83.0+1.20.1"
}
